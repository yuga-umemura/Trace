services:
  postgres:
    image: postgres:16
    container_name: trace-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: trace
      POSTGRES_PASSWORD: trace
      POSTGRES_DB: trace
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
